package org.pointyware.artes.services.openai.network

/**
 * OpenAI Credentials would not be deployed with an app so we won't load from resources or assets.
 * Credentials would be configured by a user in an app and passed to our API.
 *
 *
 */
